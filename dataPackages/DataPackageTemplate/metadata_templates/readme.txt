Folder where information used in the generation of the metadata file is stored.
Two additional files (attributes_xxx and catvars_xxx) are generated when running the package-generation script and need to be edited before final package preparation.
keywords and personnel files should also be edited prior to publication.

methods.txt and abstratct.txt are auto-generated from report parameters.